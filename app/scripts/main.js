'use strict';var app = (function(document, $, _) {    var _init = function() {            var categoryTempate = _.template($('#category-template').html());            // Populate the templates            $.getJSON('boardData.json', function(data) {                $('.categories').html(categoryTempate(data));                _bindLinks();            });            $('#start-app').click(function() {                $('.start-screen').hide();                $('.board').show();            });        },        _bindLinks = function() {            $('.open-card').click(function() {                $(this).find('input[type=checkbox]').attr('checked', true);            });            $('.close-card').click(function() {                $(this).siblings('input[type=checkbox]').attr('checked', false);            });        };    return {        init: _init    };})(document, jQuery, _);(function() {    app.init();})();